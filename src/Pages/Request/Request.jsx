import React, { Fragment } from 'react';
import NavBar from '../../Components/NavBar/NavBar';

const Request = () => {
    return (
        <Fragment>
            <NavBar tab={'request'} />
            <div>Request</div>
        </Fragment>
    );
};

export default Request;