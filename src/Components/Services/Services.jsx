import React, { Fragment } from 'react';
import NavBar from '../NavBar/NavBar';

const Services = () => {
    return (
        <Fragment>
            <NavBar tab={'services'} />
            <div>Services</div>
        </Fragment>
    );
};

export default Services;