import React, { Fragment } from 'react';
import NavBar from '../NavBar/NavBar';

const Request = () => {
    return (
        <Fragment>
            <NavBar tab={'request'} />
            <div>Request</div>
        </Fragment>
    );
};

export default Request;